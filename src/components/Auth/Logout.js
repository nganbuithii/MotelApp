import { Button } from "react-native";

const Logout = () => {
    const logout =() => {
        
    }
    return (
        <Button title="đăng xuât" onPress={logout}/>
    )
}

export default Logout;